# Stage A result

The exact contemporaneous control reproduced the historical second-focus
reread. The dedicated maintenance call then produced a 169-token bounded
progress state with a normal stop, complete fields, no fabricated identity,
and no unsupported completion claim.

Direct audit qualifies the state for a downstream utility test with two
limitations preserved verbatim: the next-progress field expresses a readiness
disposition with two outcomes rather than one preselected action, and the
do-not-repeat field preserves task prohibitions without explicitly naming the
historical focus reread.

The complete bound package costs 459 content tokens and 464 marginal prompt
tokens. The treated actor packet is 20,563 tokens and leaves 429 tokens beyond
the unchanged 4,096-token response reserve. The semantic payload is 36.4% of
the actual marginal prompt increment.

Stage A consumed two calls with zero retries. Replay passed all 18 checks. The
server stopped, its port closed, and GPU memory returned. These results qualify
expression and transport only; actor utility remains untested until Stage B.
