# Treatment delta

## C0 control

No change. Replay the exact donor request.

## M1 maintenance

Change only the invocation mode:

- replace the actor system instruction with a dedicated progress-maintenance
  instruction;
- preserve the other seven donor messages exactly;
- append one five-field maintenance instruction;
- remove the actor JSON action grammar for this call;
- set maximum completion to 384 tokens.

M1 does not take a task action.

## Conditional T1 actor treatment

Not yet materialized. After direct audit, append one canonical package to the
unchanged eight-message actor request. The full package—not the prose alone—is
the treatment. No evidence, chronology, tool, candidate, sampler, reserve, or
action-schema byte may otherwise change.
